pairs=[(EId,CId,time,FramesNotUpdated)]


def CalDist(Eid):

    return mindist,Cid
def AssignPairs():
    for each employee in employees.id
        minDist,Cid=CalDist(EId):
        if minDist >= Min_Dist_threshold:
            if eid,cid exist in pairs
                this pair.time+=0.2          //0.2 is 1/n_frames
            else:
                pairs.append(Eid,Cid,time=0.2,framesNotUpdate=0)
            

def UpdateFrames():
    for each pair in pairs:
     if pair.frameNotUpdated < 50:                 //50 is (min_time_threshold * n_frames)
        pair.frameNotUpdated+=1

     if pair.frameNotUpdated=50 and pair.time<min_time_threshold
        pair.pop()

<!--                                        
    at the end of the day the paris will look like this :
    pairs[
    (1,2,15 mins ,frameNotUpdated=50),
    (3,14,10 mins ,frameNotUpdated=50),
    (1,4,11 mins ,frameNotUpdated=50)
    ]    
         
         
-->
        

        
    